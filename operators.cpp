#include <iostream>

using namespace std;

main() {
    /* float a = 10;
    int b = 4;

    cout << "a + b = " << a + b << endl; //addition operation
    cout << "a - b = " << a - b << endl; //subtraction operation
    cout << "a * b = " << a * b << endl; //multiplication operation
    cout << "a / b = " << a / b << endl; //division operation

    //10 % 4 = 2 - remainder of division - modulo
    cout << "10 % 4 = "<< 10 % 4 << endl; //modulo operation */

    int c;

    //incrementation
    c = 2;
    c++; //variable++ adds one to variable
    cout << "c = " << c << endl;

    c = 2;
    c += 3; //variable+= x adds x to variable
    cout << "c = " << c << endl;

    //decrementation
    c = 2;
    c--; //variable-- subtracts one from variable
    cout << "c = " << c << endl;

    c = 2;
    c -= 3; //variable -= x subtracts x from variable
    cout << "c = " << c << endl;

    //there is also *=, /=, %=, etc.
}
