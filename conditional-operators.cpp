#include <iostream>

using namespace std;

main() {
    int a = 10, b = 20;

    //CONDITION ? instruction true : instruction false
    string message = (a > b) ? "a > b" : "a < b";

    cout << message << endl;
}
